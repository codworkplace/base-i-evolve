import psycopg2

# ????? ?????? ??????
url = "postgresql://bsuser:bspassword@localhost:5432/bsevolve"
print(f"????? ??????: {len(url)}")
print(f"???????: {list(url)}")

try:
    conn = psycopg2.connect(url)
    print("? ???????!")
    conn.close()
except Exception as e:
    print(f"??????: {e}")
